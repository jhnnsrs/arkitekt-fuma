import React from "react";

// Stub: the interactive GraphiQL explorer has not been ported yet.
export const Graph = (_props: Record<string, unknown>) => {
  return (
    <div className="my-4 rounded-md border border-fd-border bg-fd-card p-4 text-sm text-fd-muted-foreground">
      Interactive GraphQL explorer is not available in this documentation build.
    </div>
  );
};

export default Graph;
